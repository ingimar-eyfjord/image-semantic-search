# --- Control room: private cram/timing notes, never pushed ---
# (CLAUDE.md and workflows/ ARE public — they're part of the "how this was built" showcase)
_control/

# --- Python ---
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# --- Env / secrets ---
.env
.env.*
*.key
secrets.*

# --- Data: ship the sample images so the repo runs out of the box; the index is a
#     build artifact (regenerate with `python -m src.index data/images`). ---
data/index/

# --- OS / editor ---
.DS_Store
.idea/
.vscode/
